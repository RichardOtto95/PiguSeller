import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:pigu_seller/app/modules/order_pad/order_pad_controller.dart';
import 'package:pigu_seller/app/modules/order_pad/order_pad_module.dart';

void main() {
  initModule(OrderPadModule());
  // OrderPadController orderpad;
  //
  setUp(() {
    //     orderpad = OrderPadModule.to.get<OrderPadController>();
  });

  group('OrderPadController Test', () {
    //   test("First Test", () {
    //     expect(orderpad, isInstanceOf<OrderPadController>());
    //   });

    //   test("Set Value", () {
    //     expect(orderpad.value, equals(0));
    //     orderpad.increment();
    //     expect(orderpad.value, equals(1));
    //   });
  });
}
