import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu_seller/app/modules/bill_request/bill_request_page.dart';

void main() {
  testWidgets('BillRequestPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(BillRequestPage(title: 'BillRequest')));
    //  final titleFinder = find.text('BillRequest');
    //  expect(titleFinder, findsOneWidget);
  });
}
