import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu_seller/app/modules/table_selection/table_selection_page.dart';

void main() {
  testWidgets('TableSelectionPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TableSelectionPage(title: 'TableSelection')));
    //  final titleFinder = find.text('TableSelection');
    //  expect(titleFinder, findsOneWidget);
  });
}
