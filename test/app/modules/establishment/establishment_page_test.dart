import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pigu_seller/app/modules/establishment/establishment_page.dart';

void main() {
  testWidgets('EstablishmentPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EstablishmentPage(title: 'Establishment')));
    //  final titleFinder = find.text('Establishment');
    //  expect(titleFinder, findsOneWidget);
  });
}
